%table{:class => "table table-striped table-bordered table-condensed"}
  %thead
    %tr
      %th User ID
      %th Email
      %th Work Bench ID
      %th First Name
      %th Last Name
  %tbody
    - @users.each do |user|
      %tr
        %td= user.id
        %td= user.email
        %td= user.work_id
        %td= user.first_name
        %td= user.last_name
        %td= link_to "Show", user
        %td= link_to "Edit", edit_user_registration_path(user),:id => "edit_" + user.id.to_s
        %td= link_to "Delete", user, :confirm => 'Are you sure?', :method => :delete, :id => "delete_" + user.id.to_s